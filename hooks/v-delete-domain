#!/bin/bash
# =============================================================================
# HestiaCP → Bunny DNS  –  Hook: v-delete-domain
# /usr/local/hestia/data/hooks/v-delete-domain
# https://komorebihost.com
#
# Called by HestiaCP after a domain is successfully deleted.
# Args: USER DOMAIN
# =============================================================================

USER="$1"
DOMAIN="$2"

[ -z "$USER" ] || [ -z "$DOMAIN" ] && exit 0

ENGINE="/usr/local/hestia/plugins/bunny-dns/bunny-dns.sh"
[ -x "$ENGINE" ] || exit 0

"$ENGINE" delete "$DOMAIN" "$USER" &
exit 0
